{
  "name": "iac-sandbox",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "test": "npm test --prefix web"
  },
  "devDependencies": {}
}
