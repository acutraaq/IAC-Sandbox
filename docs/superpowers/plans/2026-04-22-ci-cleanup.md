# CI Cleanup & Optimization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix stale gitignore paths, upgrade zod in functions to v4, and split the monolithic CI workflow into two path-filtered files so each job only runs when its code changes.

**Architecture:** Three independent changes applied in dependency order — gitignore first (no risk), zod upgrade second (verifiable locally), CI split last (requires both sides to be clean). Each change gets its own commit.

**Tech Stack:** GitHub Actions, zod v4, Node 24, azure/functions-action@v2, azure/webapps-deploy@v3

**Spec:** `docs/superpowers/specs/2026-04-22-ci-cleanup-design.md`

---

## File Map

| File | Action |
|---|---|
| `.gitignore` | Modify — fix 4 stale paths |
| `functions/.gitignore` | Create — ignore `dist/` and `node_modules/` |
| `functions/package.json` | Modify — zod `^3.23.0` → `^4.3.6` |
| `functions/package-lock.json` | Regenerated by npm |
| `.github/workflows/ci.yml` | Delete |
| `.github/workflows/web.yml` | Create — web job with path filter |
| `.github/workflows/functions.yml` | Create — functions job with path filter |

---

## Task 1: Fix Gitignore Files

**Files:**
- Modify: `.gitignore`
- Create: `functions/.gitignore`

- [ ] **Step 1: Fix the 4 stale paths in root `.gitignore`**

Open `.gitignore`. The current stale block (lines 6–10) reads:

```gitignore
# Build outputs
/frontend/.next/
/frontend/out/
/frontend/build/
/backend/dist/
```

Replace with:

```gitignore
# Build outputs
/web/.next/
/web/out/
/web/build/
/functions/dist/
```

- [ ] **Step 2: Verify `.next/` is now gitignored**

Run from repo root:

```bash
git check-ignore -v web/.next/
```

Expected output:
```
.gitignore:7:/web/.next/	web/.next/
```

- [ ] **Step 3: Verify `functions/dist/` is now gitignored**

```bash
git check-ignore -v functions/dist/
```

Expected output:
```
.gitignore:10:/functions/dist/	functions/dist/
```

- [ ] **Step 4: Create `functions/.gitignore`**

Create the file `functions/.gitignore` with this exact content:

```gitignore
dist/
node_modules/
```

- [ ] **Step 5: Commit**

```bash
git add .gitignore functions/.gitignore
git commit -m "chore: fix stale gitignore paths and add functions/.gitignore"
```

---

## Task 2: Upgrade Zod in Functions (v3 → v4)

**Files:**
- Modify: `functions/package.json`
- Regenerated: `functions/package-lock.json`

All zod usage in functions (`env.ts`, `deployment.schema.ts`) uses only `z.object`, `z.string`, `z.enum`, `z.literal`, `z.discriminatedUnion`, `z.record`, `z.infer`, `.safeParse`, `.default`, `.min`, `.regex` — all of these have identical APIs in zod v4. No source code changes are required.

- [ ] **Step 1: Update zod version in `functions/package.json`**

In `functions/package.json`, change:

```json
"zod": "^3.23.0"
```

to:

```json
"zod": "^4.3.6"
```

- [ ] **Step 2: Install the updated dependency**

```bash
cd functions && npm install
```

Expected: `package-lock.json` updated, `node_modules/zod` now at v4.x.

- [ ] **Step 3: Verify type check passes**

```bash
npx tsc --noEmit
```

Expected: no output, exit code 0.

- [ ] **Step 4: Verify build passes**

```bash
npm run build
```

Expected: TypeScript compiles to `dist/` with no errors.

- [ ] **Step 5: Commit**

```bash
cd ..
git add functions/package.json functions/package-lock.json
git commit -m "chore: upgrade zod in functions from v3 to v4"
```

---

## Task 3: Split CI Into Path-Filtered Workflow Files

**Files:**
- Delete: `.github/workflows/ci.yml`
- Create: `.github/workflows/web.yml`
- Create: `.github/workflows/functions.yml`

- [ ] **Step 1: Create `.github/workflows/web.yml`**

Create the file with this exact content:

```yaml
name: Web

on:
  push:
    branches: [main]
    paths:
      - 'web/**'
      - '.github/workflows/web.yml'
  pull_request:
    branches: [main]
    paths:
      - 'web/**'
      - '.github/workflows/web.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  web:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: web

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npx tsc --noEmit

      - name: Test
        run: npm run test:run

      - name: Build
        run: npm run build
        env:
          AZURE_SUBSCRIPTION_ID: 00000000-0000-0000-0000-000000000000
          AZURE_TENANT_ID: 00000000-0000-0000-0000-000000000000
          AZURE_STORAGE_CONNECTION_STRING: DefaultEndpointsProtocol=https;AccountName=build;AccountKey=AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA==;EndpointSuffix=core.windows.net

      - name: Prune dev dependencies
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: npm prune --omit=dev

      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v3
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        with:
          app-name: epf-experimental-sandbox-playground
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: web/
```

- [ ] **Step 2: Create `.github/workflows/functions.yml`**

Create the file with this exact content:

```yaml
name: Functions

on:
  push:
    branches: [main]
    paths:
      - 'functions/**'
      - '.github/workflows/functions.yml'
  pull_request:
    branches: [main]
    paths:
      - 'functions/**'
      - '.github/workflows/functions.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  functions:
    name: Functions
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: functions

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: functions/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Build
        run: npm run build

      - name: Prune dev dependencies
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: npm prune --omit=dev

      - name: Create deployment package
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: zip -r ../functions-deploy.zip dist host.json package.json node_modules

      - name: Deploy to Azure Functions
        uses: azure/functions-action@v2
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        with:
          app-name: epf-sandbox-functions
          publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
          package: functions-deploy.zip
```

- [ ] **Step 3: Delete the old combined workflow**

```bash
rm .github/workflows/ci.yml
```

- [ ] **Step 4: Verify YAML syntax on both files**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/web.yml'))" && echo "web.yml OK"
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/functions.yml'))" && echo "functions.yml OK"
```

Expected:
```
web.yml OK
functions.yml OK
```

- [ ] **Step 5: Commit**

```bash
git add .github/workflows/web.yml .github/workflows/functions.yml
git rm .github/workflows/ci.yml
git commit -m "chore: split CI into path-filtered web.yml and functions.yml workflows"
```

---

## Acceptance Criteria Verification

After all three tasks:

- [ ] `git check-ignore -v web/.next/` returns `.gitignore:7:/web/.next/`
- [ ] `git check-ignore -v functions/dist/` returns `.gitignore:10:/functions/dist/`
- [ ] `functions/package.json` shows `"zod": "^4.3.6"`
- [ ] `cd functions && npx tsc --noEmit` exits 0
- [ ] `cd functions && npm run build` exits 0
- [ ] `.github/workflows/ci.yml` does not exist
- [ ] `.github/workflows/web.yml` exists with `paths: ['web/**', '.github/workflows/web.yml']`
- [ ] `.github/workflows/functions.yml` exists with `paths: ['functions/**', '.github/workflows/functions.yml']`
- [ ] `web.yml` Test step uses `npm run test:run` (not `npm test`)
