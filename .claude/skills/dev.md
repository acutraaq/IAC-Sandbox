---
name: dev
description: Start the IAC Sandbox dev server on localhost:3000
---

# Start Dev Server

```bash
cd web && npm run dev
```

The dev server runs at **http://localhost:3000**.

API routes are served at `/api/deployments`, `/api/healthz` — no separate backend needed.

To stop: `Ctrl+C`.
