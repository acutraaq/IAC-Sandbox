# Design System

Auto-loaded when editing any `web/` file.

Full design system (colors, typography, elevation, components, motion) lives in [`DESIGN.md`](../../DESIGN.md) at the repo root — read that file, not a copy here. Keeping one copy avoids the two files silently drifting apart.
